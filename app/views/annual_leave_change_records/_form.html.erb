<%= form_for(@record) do |f| %>
    <% if @record.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@record.errors.count, "error") %> prohibited this annual leave change record from being saved:</h2>

            <ul>
                <% @record.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="field">
        <%= f.label '变动类型' %><br>
        <%= f.text_field :kind %>
    </div>
    <div class="field">
        <%= f.label '日期' %><br>
        <%= f.text_field :when %>
    </div>
    <div class="field">
        <%= f.label '天数' %><br>
        <%= f.text_field :number %>
    </div>
    <div class="field">
        <%= f.label '员工ID' %><br>
        <%= f.text_field :employee_id %>
    </div>
    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>
